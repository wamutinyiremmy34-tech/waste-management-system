[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = [
    "B008",  # FastAPI's Depends()/require_roles() in argument defaults is the documented idiom
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["F401"]
